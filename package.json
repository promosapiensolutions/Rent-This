{
  "name": "rent-this-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "python3 -m http.server 4173",
    "build": "rm -rf public && mkdir -p public && cp -R index.html styles.css app.js assets public/"
  }
}
